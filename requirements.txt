# LLM
ollama>=0.3.0

# Speech to Text (faster than openai-whisper on CPU)
faster-whisper>=1.0.0

# Wake Word (free, no API key required)
openwakeword>=0.6.0

# Audio capture — pyaudio WASAPI (same API as Zoom/Chrome)
pyaudio>=0.2.14
SpeechRecognition>=3.10.0
numpy>=1.24.0
scipy>=1.10.0

# Wake word still needs sounddevice for openwakeword model loading
sounddevice==0.4.6

# Audio playback — use 1.2.2 specifically; 1.3.0 has Windows bugs
playsound==1.2.2

# Memory (vector DB)
chromadb>=0.5.0

# Document reading
pdfplumber>=0.10.0
python-docx>=1.0.0

# Global hotkey
keyboard>=0.13.5

# Web search + browser (Phase 3)
ddgs>=1.0.0
playwright>=1.40.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
rapidfuzz>=3.0.0
